{% extends "@Secret/SecretView/bootstrap.html.twig" %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}


    <div>
        <button name='kartofel' type='submit' value='kartofel' class='btn btn-default'>Produkt save</button>
        <a href="{{ path('welcomePage') }}" class="btn btn-default">Wróć do strony głównej</a>
    </div>
    {{ form_end(form) }}

{% endblock %}